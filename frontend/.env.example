# Copia este archivo a `.env` para que la SPA consuma la API JSON del backend PHP.
# Con VITE_API_BASE definido (aunque esté vacío) la SPA usa /api/... a través del proxy de Vite.
# Sin este archivo, la SPA usa el catálogo local de respaldo.
VITE_API_BASE=
